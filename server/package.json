{
  "name": "shadowstrike-leaderboard",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute shadowstrike-scores --local --file=./migrations/0001_init.sql",
    "db:remote": "wrangler d1 execute shadowstrike-scores --remote --file=./migrations/0001_init.sql",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240924.0",
    "wrangler": "^3.78.0"
  }
}
